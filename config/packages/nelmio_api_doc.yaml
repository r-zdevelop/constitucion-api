nelmio_api_doc:
    documentation:
        info:
            title: LexEcuador API
            description: API REST para consulta de la Constitución de Ecuador
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc$)
